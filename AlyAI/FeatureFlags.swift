import Foundation

struct FeatureFlags {
    /// Temporarily disable Superwall paywall for development iteration.
    /// Set to `false` for production behavior.
    static let disablePaywallForDevelopment = true
}
